fn main() {
    println!("Hello, cargo!");
    fn init_inventory_matrix() {
        println!("initialized inventory matrix")
    }
    init_inventory_matrix();
}
const MAT_COLS: u16 = 7; // The number of columns in any matrix of a domain(domains are WIP, Inventory, etc.). 
